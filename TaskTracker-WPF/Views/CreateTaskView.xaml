<UserControl x:Class="TaskTracker_WPF.Views.CreateTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskTracker_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/> <!-- header -->
            <RowDefinition Height="auto"/> <!-- date -->
            <RowDefinition Height="auto"/> <!-- title -->
            <RowDefinition Height="auto"/> <!-- title descrption -->
            <RowDefinition Height="auto"/> <!-- task completion -->
            <RowDefinition Height="auto"/> <!-- buttons -->
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Create New Task" 
            Style="{StaticResource Header}"/>
            <!--FontSize="24"-->

        <Grid Grid.Row="1" Margin="0 15 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock 
                Grid.Row="0"
                Text="Date"/>

            <DatePicker
                Grid.Row="1"
                SelectedDate="{Binding Path=DateOfDay, UpdateSourceTrigger=PropertyChanged}"
                Width="150"
                HorizontalAlignment="Left"
                Margin="0 5 0 0"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock 
                Grid.Row="0"
                Text="Title"/>

            <TextBox
                Grid.Row="1"
                Text="{Binding TaskTitle, UpdateSourceTrigger=PropertyChanged}"
                Width="250"
                HorizontalAlignment="Left"
                Margin="0 5 0 0"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Text="Description"/>
            <TextBox
                Grid.Row="1"
                Text="{Binding TaskDescription, UpdateSourceTrigger=PropertyChanged}"
                Width="400"
                Height="80"
                HorizontalAlignment="Left"
                Margin="0 5 0 0"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Completed:"/>
            <CheckBox
                Grid.Column="1"
                Margin="10 2 0 0"
                IsChecked="{Binding TaskCompletion}"
                />
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0 25 0 0">
            <Button Content="Create" Command="{Binding CreateCommand}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="10 0 0 0"/>
        </StackPanel>


    </Grid>
</UserControl>
